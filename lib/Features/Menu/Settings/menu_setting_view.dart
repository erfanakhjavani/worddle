import 'package:flutter/material.dart';

class MenuSettingView extends StatelessWidget {
  const MenuSettingView({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
    );
  }
}
